{
  "attack": "C7 \u2014 a replacement chain that leads into a dead end",
  "fixture": "adversarial-1.1.0 (paired against miri-1.1.0, which does not carry it)",
  "checks": [
    "MIRI-CLI-038"
  ],
  "clause": "clause 2 \u2014 the replacement is itself deprecated or absent",
  "note": "A consumer that follows one hop and stops reports a migration path that cannot be taken. Chains are resolved, not merely present.",
  "linter_assertion": {
    "must_report_on": {
      "arm": "adversarial-1.1.0",
      "checks": [
        "MIRI-CLI-038"
      ]
    },
    "must_not_report_on": {
      "arm": "miri-1.1.0",
      "checks": [
        "MIRI-CLI-038"
      ],
      "why": "The conforming arm does not carry this attack. A linter reporting the same check on both arms has detected nothing \u2014 it reports unconditionally, and would score identically against a fixture with no attacks at all."
    },
    "evidence": "--formal's replacement is --polite, which is itself deprecated and points on to --courteous, which does not appear in --describe at all."
  },
  "paired_control": {
    "arm": "miri-1.1.0",
    "why": "greetctl.py is byte-identical across all four arms \u2014 the builder fails if the digests diverge \u2014 so the arms differ only in the two JSON files beside it. A difference in what a linter reports is therefore attributable to metadata, not code. Neither arm decides the case alone: the attack arm catches a linter that reports nothing, the control arm catches one that reports everything."
  },
  "why_this_case_was_missing": "One-hop resolution passes this arm. The golden requires the chain be followed to a terminus, which is the only way to tell a resolving linter from a dereferencing one."
}
