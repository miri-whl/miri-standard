{
  "attack": "C2 \u2014 prose on stdout ahead of the JSON document",
  "fixture": "adversarial-1.1.0 (paired against miri-1.1.0, which does not carry it)",
  "checks": [
    "MIRI-CLI-011",
    "MIRI-CLI-026"
  ],
  "clause": "011 clause 2; 026's passive-by-default rule",
  "note": "Also the passive-by-default violation: a command that was not asked about updates reports them, on the machine channel. Both checks fire; reporting only 011 misses that the tool checks for updates unbidden.",
  "linter_assertion": {
    "must_report_on": {
      "arm": "adversarial-1.1.0",
      "checks": [
        "MIRI-CLI-011",
        "MIRI-CLI-026"
      ]
    },
    "must_not_report_on": {
      "arm": "miri-1.1.0",
      "checks": [
        "MIRI-CLI-011",
        "MIRI-CLI-026"
      ],
      "why": "The conforming arm does not carry this attack. A linter reporting the same check on both arms has detected nothing \u2014 it reports unconditionally, and would score identically against a fixture with no attacks at all."
    },
    "evidence": "Every JSON-mode stdout is prefixed with an update banner, so a strict parse fails on all of them."
  },
  "paired_control": {
    "arm": "miri-1.1.0",
    "why": "greetctl.py is byte-identical across all four arms \u2014 the builder fails if the digests diverge \u2014 so the arms differ only in the two JSON files beside it. A difference in what a linter reports is therefore attributable to metadata, not code. Neither arm decides the case alone: the attack arm catches a linter that reports nothing, the control arm catches one that reports everything."
  },
  "why_this_case_was_missing": "The banner corrupts every command at once, so a linter that samples a single command still sees it. That makes it the WEAKEST discriminator in the set, and worth stating as such rather than counting it as strong evidence."
}
