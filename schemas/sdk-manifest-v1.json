{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://miri-whl.github.io/schemas/sdk-manifest-v1.json",
  "title": "Miri Standard SDK Manifest",
  "description": "Schema for sdk-manifest.json - Core API index with structured signatures and metadata",
  "type": "object",
  "required": [
    "sdk_version",
    "generated_at",
    "miri_version",
    "quick_reference",
    "api_index"
  ],
  "properties": {
    "sdk_version": {
      "type": "string",
      "description": "Version of the SDK package (PEP 440); accepts epoch, multi-segment release, pre/post/dev, and local versions so it never rejects a valid wheel version",
      "pattern": "^v?([0-9]+!)?[0-9]+(\\.[0-9]+)*([._-]?(a|b|c|rc|alpha|beta|pre|preview)[._-]?[0-9]*)?([._-]?(post|rev|r)[._-]?[0-9]*)?([._-]?dev[._-]?[0-9]*)?(\\+[a-z0-9]+([._-][a-z0-9]+)*)?$"
    },
    "generated_at": {
      "type": "string",
      "format": "date-time",
      "description": "ISO 8601 timestamp when metadata was generated"
    },
    "miri_version": {
      "type": "string",
      "description": "Version of Miri Standard used",
      "pattern": "^\\d+\\.\\d+$"
    },
    "quick_reference": {
      "type": "object",
      "required": [
        "primary_classes",
        "key_methods",
        "common_imports"
      ],
      "properties": {
        "primary_classes": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "description": "Main classes agents should know about"
        },
        "key_methods": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "description": "Most important methods across all classes"
        },
        "common_imports": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "description": "Standard import statements for the SDK"
        },
        "typical_workflow": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "description": "Common sequence of operations"
        }
      }
    },
    "code_index": {
      "type": "object",
      "required": ["path", "format"],
      "additionalProperties": false,
      "description": "Where a precomputed code index lives in this wheel and what format it is in, so a consumer can DISCOVER it and decide whether it can read it before opening it. Optional, and MIRI-PY-045 is a SHOULD: no toolchain produces these for wheels yet. The standard names the format and not the producer, exactly as it names the wheel format and not the build backend. Declaring the format is the load-bearing part - a protobuf index is far cheaper than the source when queried for one symbol and more expensive than the source when read whole, so a consumer that cannot decode it needs to know that from the manifest rather than from a failed parse, and fall back to `api_index`.",
      "properties": {
        "path": {"type": "string", "pattern": "^\\.dist-info/", "description": "Path within the wheel, under `.dist-info/` beside the SBOM documents PEP 770 places there — one convention for precomputed artifacts rather than two. MUST be listed in RECORD like every other carried file."},
        "format": {"type": "string", "enum": ["scip"], "description": "A closed set of one. SCIP is a published format with a protobuf schema, several indexers and existing consumers; the standard adopting it rather than maintaining its own graph is the whole point of withdrawing api-graph.json. Adding a format here is a schema change, deliberately, so two wheels never describe the same thing differently."},
        "reduction": {"type": "string", "enum": ["none", "occurrences-stripped", "references-stripped"], "description": "Which sections were removed before shipping. A full index measured 62% of a 1.87 MB wheel against 20% with reference occurrences removed, so shipping one unreduced is usually wrong — but WHICH reduction is safe is unsettled: if third-party readers locate definitions through definition occurrences rather than through SymbolInformation, stripping all occurrences breaks them. Declared so a consumer knows what it was handed instead of inferring it, and so the answer can be measured across implementations before the standard prescribes one."},
        "produced_by": {"type": "string", "description": "Indexer name and version, informative. Recorded because two producers emitting different symbol IDs for the same code would break cross-release deltas, and that has not been tested across two indexers."}
      }
    },
    "api_index": {
      "type": "object",
      "minProperties": 1,
      "patternProperties": {
        "^[A-Za-z_][A-Za-z0-9_]*$": {
          "$ref": "#/definitions/api_component"
        }
      },
      "description": "Index of all public API components"
    },
    "error_handling": {
      "type": "object",
      "patternProperties": {
        "^[A-Za-z_][A-Za-z0-9_]*$": {
          "$ref": "#/definitions/error_info"
        }
      },
      "description": "Common errors and their handling"
    },
    "configuration": {
      "type": "object",
      "properties": {
        "environment_variables": {
          "type": "object",
          "patternProperties": {
            "^[A-Z_][A-Z0-9_]*$": {
              "type": "string"
            }
          },
          "description": "Environment variables used by the SDK"
        },
        "config_files": {
          "type": "object",
          "patternProperties": {
            ".*": {
              "type": "string"
            }
          },
          "description": "Configuration files and their purposes"
        }
      }
    }
  },
  "definitions": {
    "api_component": {
      "type": "object",
      "required": [
        "type",
        "purpose"
      ],
      "properties": {
        "type": {
          "type": "string",
          "enum": [
            "class",
            "function",
            "module",
            "constant"
          ]
        },
        "purpose": {
          "type": "string",
          "description": "What this component does"
        },
        "complexity": {
          "type": "string",
          "enum": [
            "beginner",
            "intermediate",
            "advanced"
          ],
          "description": "Complexity level for users"
        },
        "init_params": {
          "$ref": "#/definitions/parameters",
          "description": "Constructor parameters for classes"
        },
        "key_methods": {
          "type": "object",
          "patternProperties": {
            "^[a-z_][a-z0-9_]*$": {
              "$ref": "#/definitions/method_info"
            }
          }
        },
        "usage_patterns": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "description": "IDs of relevant usage patterns"
        },
        "related_classes": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "description": "Names of related API components"
        },
        "common_errors": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "description": "Common exception types"
        },
        "example": {
          "type": "string",
          "description": "Quick usage example"
        },
        "file": {
          "type": "string",
          "description": "Source file path"
        },
        "signature": {
          "type": "string",
          "description": "The component's call signature as it appears in source, e.g. \"Greeter(language: str = 'en')\". An AST fact. Optional: a generator emits it where the source states it, and a consumer MUST NOT assume its presence (Discovery Contract \u00a73.5)."
        }
      }
    },
    "method_info": {
      "type": "object",
      "required": [
        "purpose"
      ],
      "properties": {
        "purpose": {
          "type": "string",
          "description": "What this method does"
        },
        "params": {
          "$ref": "#/definitions/parameters"
        },
        "returns": {
          "type": "string",
          "description": "Return type and description"
        },
        "raises": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "description": "Exception types that may be raised"
        },
        "example": {
          "type": "string",
          "description": "Usage example"
        },
        "complexity": {
          "type": "string",
          "enum": [
            "beginner",
            "intermediate",
            "advanced"
          ]
        }
      }
    },
    "parameters": {
      "type": "object",
      "patternProperties": {
        "^[a-z_][a-z0-9_]*$": {
          "$ref": "#/definitions/parameter_info"
        }
      }
    },
    "parameter_info": {
      "type": "object",
      "required": [
        "type"
      ],
      "properties": {
        "type": {
          "type": "string",
          "description": "Parameter type (e.g., 'str', 'int', 'List[str]')"
        },
        "required": {
          "type": "boolean",
          "default": true,
          "description": "Whether parameter is required"
        },
        "default": {
          "description": "Default value if not required"
        },
        "description": {
          "type": "string",
          "description": "Parameter description"
        },
        "example": {
          "type": "string",
          "description": "Example value"
        }
      }
    },
    "error_info": {
      "type": "object",
      "required": [
        "when",
        "common_causes"
      ],
      "properties": {
        "when": {
          "type": "string",
          "description": "When this error occurs"
        },
        "common_causes": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "description": "Common causes of this error"
        },
        "solutions": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "description": "How to fix this error"
        }
      }
    }
  }
}
