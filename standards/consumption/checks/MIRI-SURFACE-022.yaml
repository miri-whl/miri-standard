# MIRI-SURFACE-022 — generated with the Surface Conformance profile; canonical source of truth.
id: MIRI-SURFACE-022
name: Discovers import-free; executes nothing and fetches nothing
target: surface
status: active
added_in: 0.3-draft
level: MUST
category: Boundaries
weight: 8
conditional: false
severity:
  default: CRITICAL
  violation_unit: each package imported, executed, or fetched during a query
short_description: Discovers import-free; executes nothing and fetches nothing
long_description: 'Surface obligation from the Discovery Contract. This check verifies: Discovers import-free; executes
  nothing and fetches nothing.'
examples:
  violation: 'importlib.import_module(pkg)  # to read its __doc__ for the summary'
  compliant: 'importlib.util.find_spec(pkg)  # location only; metadata read from disk, never executed'
rationale: 'Discovery runs over every installed package, including ones nobody has vetted. If locating metadata
  imports the target, a hostile package gains code execution merely by being installed and enumerated — the query
  surface becomes the attack. The same logic covers fetching: resolving a publisher-declared URL would turn publisher-controlled
  data into server-side requests.'
fires_when:
- Any query imports the target package or executes any of its code
- The surface fetches a package-declared URL such as `update_check.url` or an advisory endpoint
- A package that cannot be resolved without importing is imported rather than reported `NOT_DISCOVERABLE`
remediation:
- Resolve locations through the import system's path finders and read files; never import the target
- 'Fetch nothing: relaying the declared URL is the whole obligation, and resolving it is the consumer''s act'
suggested_fix: Resolve locations through the import system's path finders and read files; never import the target
references:
- title: Discovery Contract
  url: discovery-contract.md
  type: spec
- title: Lifecycle and Security Metadata §9.2
  url: ../python/lifecycle-security-metadata.md
  type: spec
urls:
  definition: https://github.com/miri-whl/miri-standard/blob/main/standards/consumption/checks/MIRI-SURFACE-022.yaml
  html: https://miri-whl.github.io/checks/miri-surface-022/
