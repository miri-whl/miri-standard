# MIRI-PY-024 — generated with the checklist table; committee-owned source of truth.
id: MIRI-PY-024
name: SBOM when bundling
target: python-wheel
status: active
added_in: 0.1-draft
level: MUST
category: Identity & Security
weight: 4
conditional: true
severity:
  default: HIGH
  violation_unit: the wheel's SBOM state
short_description: Wheels with non-Python components carry `.dist-info/sboms/`
long_description: 'Vulnerability and currency verdicts are computed externally by joining declared identity against advisory
  databases; a wheel that misdeclares identity or sources silently breaks that join. This check verifies: Wheels with non-Python
  components carry `.dist-info/sboms/`.'
examples:
  violation: The wheel bundles a vendored OpenSSL but ships no .dist-info/sboms/
  compliant: auditwheel-generated CycloneDX SBOM in .dist-info/sboms/ covers the vendored OpenSSL
suggested_fix: Let auditwheel (>=6.5.0) generate PEP 770 SBOM data, or add it in the build.
references:
- title: PEP 770
  url: https://peps.python.org/pep-0770/
  type: external
- title: Lifecycle §3.2
  url: lifecycle-security-metadata.md
  type: spec
