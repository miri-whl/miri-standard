# MIRI-CLI-041 — generated with the checklist table; committee-owned source of truth.
id: MIRI-CLI-041
name: Read/write visible
target: cli
status: active
added_in: 0.1-draft
level: MUST
category: Safety
weight: 2
conditional: false
severity:
  default: MEDIUM
  violation_unit: each ambiguous command
  population_unit: all commands in --describe
short_description: The read-vs-write nature of each command is determinable from `--describe`/`--help` alone
long_description: 'Agents retry, parallelize, and act without human review between steps; mutations must be previewable,
  marked, and safe to repeat. This check verifies: The read-vs-write nature of each command is determinable from `--describe`/`--help`
  alone.'
examples:
  violation: Whether 'tool sync' writes is not determinable from --describe
  compliant: Each command's describe entry declares whether it reads or writes
suggested_fix: Make the read-vs-write nature explicit per command.
references:
- title: Landscape §2.2 (anc.dev)
  url: landscape-and-prior-art.md
  type: spec
